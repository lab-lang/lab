# Starter Lab package

First [install the `lab` CLI](../../crates/lab-cli/README.md). Then, from this directory:

```sh
lab check
lab metadata
lab build
```

The build writes portable module artifacts beneath `.lab/build/`.
