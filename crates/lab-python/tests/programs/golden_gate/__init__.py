"""The Golden Gate example's designs, written with the Lab Python SDK.

Each Python module here opens the Lab module of the same name, so the package
mirrors `examples/golden-gate/src/designs/` declaration for declaration. The
round-trip test checks that it does.
"""
