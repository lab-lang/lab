name: Release

on:
  push:
    tags: ['v*.*.*']

permissions:
  contents: read

jobs:
  check-tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify tag matches workspace version
        run: |
          tag="${GITHUB_REF_NAME#v}"
          version="$(sed -n 's/^version = "\(.*\)"$/\1/p' Cargo.toml | head -1)"
          if [ "$tag" != "$version" ]; then
            echo "tag v$tag does not match" \
              "workspace.package.version $version" >&2
            exit 1
          fi

  build:
    needs: check-tag
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-apple-darwin
            os: macos-14
          - target: x86_64-apple-darwin
            os: macos-14
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-24.04-arm
          - target: x86_64-pc-windows-msvc
            os: windows-latest
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build
        run: >
          cargo build --release --target ${{ matrix.target }}
          -p lab-cli -p lab-lair

      - name: Package (unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          dir="target/${{ matrix.target }}/release"
          asset="lab-${{ matrix.target }}.tar.gz"
          tar -czf "$asset" -C "$dir" lab labc lab-opt
          if command -v shasum >/dev/null 2>&1; then
            shasum -a 256 "$asset" > "$asset.sha256"
          else
            sha256sum "$asset" > "$asset.sha256"
          fi

      - name: Package (windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $dir = "target/${{ matrix.target }}/release"
          $asset = "lab-${{ matrix.target }}.tar.gz"
          tar -czf $asset -C $dir lab.exe labc.exe lab-opt.exe
          $hash = (Get-FileHash $asset -Algorithm SHA256).Hash.ToLower()
          # LF, not Out-File's CRLF, so SHA256SUMS has one line ending once
          # every target's .sha256 file is concatenated together.
          [System.IO.File]::WriteAllText("$asset.sha256", "$hash  $asset`n")

      - uses: actions/upload-artifact@v4
        with:
          name: lab-${{ matrix.target }}
          path: |
            lab-${{ matrix.target }}.tar.gz
            lab-${{ matrix.target }}.tar.gz.sha256
          if-no-files-found: error

  # One abi3 wheel per platform covers CPython 3.11 and newer, so the matrix
  # is platforms rather than platforms times interpreter versions. The sdist
  # carries the workspace crates the bindings are built from, so installing
  # from source works anywhere a Rust toolchain does.
  wheels:
    needs: check-tag
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-apple-darwin
            os: macos-14
          - target: x86_64-apple-darwin
            os: macos-14
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-24.04-arm
          - target: x86_64-pc-windows-msvc
            os: windows-latest
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: PyO3/maturin-action@v1
        with:
          command: build
          target: ${{ matrix.target }}
          args: --release --out dist --manifest-path crates/lab-python/Cargo.toml
          # The Linux wheels build in manylinux containers so they carry a
          # tag pip will install on any reasonably current distribution.
          manylinux: auto

      - uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.target }}
          path: dist/*.whl
          if-no-files-found: error

  sdist:
    needs: check-tag
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: PyO3/maturin-action@v1
        with:
          command: sdist
          args: --out dist --manifest-path crates/lab-python/Cargo.toml

      - uses: actions/upload-artifact@v4
        with:
          name: wheels-sdist
          path: dist/*.tar.gz
          if-no-files-found: error

  # Trusted publishing: PyPI is configured to accept uploads from this
  # workflow in this repository, so there is no API token to hold. The
  # environment is what a protection rule can require a review on.
  pypi:
    needs: [wheels, sdist]
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          pattern: wheels-*
          merge-multiple: true

      - uses: pypa/gh-action-pypi-publish@release/v1

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Assemble checksums
        run: cat dist/*.sha256 > dist/SHA256SUMS

      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            dist/*.tar.gz dist/SHA256SUMS \
            --title "$GITHUB_REF_NAME" \
            --generate-notes
